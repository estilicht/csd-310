import pymysql

CREATE TABLE team(team_id INTEGER NOT NULL, team_name STR NOT NULL, mascot STR NOT NULL);
INSERT INTO team VALUE ("85962", "Mets", "Mr. Met");

CREATE TABLE players(player_id INTEGER NOT NULL, first_name STR NOT NULL, last_name STR NOT NULL);
INSERT INTO players ("65", "Trevor","May");
INSERT INTO players ("67", "Seth", "Lugo");
